import numpy as np

# Get input for exam1 scores
exam1_scores = list(map(int, input().split()))

# Get input for exam2 scores
exam2_scores = list(map(int, input().split()))

# Convert lists to numpy arrays


# Add the two arrays to get total scores
total_scores = np.add(exam2_scores,exam1_scores)# Your code here

# Print the total scores
print(total_scores)
